<div class="overlay" id="overlay" onclick="closeSidebar()"></div>

<nav class="sidebar col-md-2 col-lg-2" id="sidebar">
        
    <button class="close-btn" onclick="closeSidebar()">
        <i class="fas fa-times"></i>
    </button>

    <div class="user-info">
        <div class="user-avatar">
            <span class="user-initials">
                <%= user && user.name_users ? user.name_users[0].toUpperCase() : '?' %> 
            </span>
        </div>
        <div class="user-details">
          <p class="user-perfil">
            <strong>
                <%= user && user.profile ? user.profile.name_profile : 'Perfil não definido' %>
            </strong>
        </p>
          <p class="user-loja">
            <strong>
                <%= user && user.store ? user.store.name_store : 'Loja não definida' %>
            </strong>
        </p>
        </div>
    </div>
        
    <div class="menu">
        <ul class="nav flex-column">

            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/main/page">
                    <i class="fas fa-home" style="margin-right: 8px"></i>
                        Main
                </button>
            </li>
            
            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/users/page">
                    <i class="fas fa-users" style="margin-right: 8px"></i>
                        Usuários
                </button>
            </li>

            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/profiles/page">
                    <i class="fas fa-user-shield" style="margin-right: 8px"></i>
                        Perfis de Acesso
                </button>
            </li>

            
            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/talons/page">
                    <i class="fas fa-file-invoice" style="margin-right: 8px"></i>
                        Talões
                </button>
            </li>
           
            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/stocks/page">
                    <i class="fas fa-box" style="margin-right: 8px"></i>
                        Estoque
                </button>
            </li>

            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/stores/page">
                    <i class="fas fa-store" style="margin-right: 8px"></i>
                        Lojas
                </button>
            </li>

            <li class="nav-item">
                <button class="btn btn-custom w-100 mb-2 load-content" data-url="/reports/page">
                    <i class="fas fa-th-large" style="margin-right: 8px"></i>
                        Relatórios
                </button>
            </li>
            
        </ul>
    </div>

    <div class="bottom-links">
          
        <button 
            class="btn btn-custom w-100 mb-2 load-content editPassword"
            data-registration="<%= user && user.registration_users ? user.registration_users : '' %>"
            data-url="/config/password">
            <i class="fas fa-cog" style="margin-right: 8px"></i>
                Configurações
        </button>
          
        <button class="btn btn-danger w-100 mb-2"  id="logoutBtn">
            <i class="fas fa-sign-out-alt" style="margin-right: 8px"></i>
                Sair
        </button>
    </div>
</nav>