// Controlador para a página de envios
const sendingController = {
    getUserPage: (req, res) => {
        res.send("Página dos Envios de Talões");
    },

};

module.exports = sendingControllerController;
